import styled from 'styled-components';

const Home = (props) => {
    return (
        <Wrapper>
            <Content>
                Home
            </Content>
        </Wrapper>
    )
}

const Wrapper = styled.div``

const Content = styled.div``

export default Home;
