import styled from 'styled-components';

const Home = (props) => {
    return (
        <Wrapper>
            <Content>
                Home
            </Content>
        </Wrapper>
    )
}

const Wrapper = styled.div`
    background: #000;
    color: #FFF;
    height: 100vh;
`

const Content = styled.div``

export default Home;
